# 1st proposal

    can we try make a schedule manager app that receive input from the hospital front and evaluate priority by itself according to how many case are there, how critical it is, how long does it take, where can multiple case be assigned to multiple doctor or facility without conflicting and learn from push sensor in hospital door or utilize motion sensor from ESP32 to tell the model if this facility is available to assign patient to

    and also let's try using an already existing AI model via API to help answer with some normal question (We also need a way to make sure patient information won't be leak through this API usage)